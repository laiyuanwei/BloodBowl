/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package testPlayers;

import bloodbowl.model.map.Cell;
import bloodbowl.model.players.Blitzer;
import bloodbowl.model.players.PlayerType;
import bloodbowl.model.players.Skills;
import junit.framework.Assert;
import junit.framework.TestCase;

import java.util.ArrayList;


/**
 *
 * @author Yuanwei Lai
 */
public class BlitzerTest extends TestCase {
    ArrayList<Skills> skillsList = new ArrayList<Skills>();
    private Blitzer blitzer=null;
    private Cell cell=null;
    private Cell newcell=null;
    private Cell clickedCell=null;
    private Cell location=null;


    public BlitzerTest(String testName) {
        super(testName);
    }

    @Override
    protected void setUp() throws Exception {
        super.setUp();
       // skill=Skills.PASS;
        blitzer=new Blitzer(1,2,3,4,"Test Blitzer",10,skillsList,PlayerType.BLITZER);
        cell=new Cell(1,2);
        newcell=new  Cell(1,2);
       // location=new Cell(1,2);
       // clickedCell=new Cell(3,4);
    }

    @Override
    protected void tearDown() throws Exception {
        super.tearDown();
    }

    // TODO add test methods here. The name must begin with 'test'. For example:
    // public void testHello() {}

    public void testResetMovementLeft(){
       blitzer.resetMovementLeft();
       Assert.assertEquals(1, blitzer.getMa());
    }

    public void testGetMaLeft(){
       Assert.assertEquals(1, blitzer.getMa());
    }

    public void testDecreaseMovementLeft()
    {
        blitzer.decreaseMovementLeft();
        Assert.assertEquals(0, blitzer.getMa()-1);
        //maLeft--;
    }
   public void testCanMove()
    {
        boolean expected;
        if(blitzer.getMa()>0)
            expected=true;
        else
            expected=false;
       Assert.assertEquals(expected,blitzer.canMove());
    }
    public void testSetTeamID()
    {
        blitzer.setTeamID(5);
        Assert.assertEquals(5,blitzer.getTeamID());

        //teamID=id;
    }

    public void  testGetTeamID()
    {

        Assert.assertEquals(10, blitzer.getTeamID());
        //return teamID;
    }
    public void testSetLocation()  //Method for relocation during gameplay
    {
        cell.resetPlayer();
        //cell = (PitchCell) newcell;
        cell.setPlayer();
        newcell.setPlayer();
        blitzer.setActive();
        //blackorc.setChanged();
        Assert.assertEquals(true, cell.hasPlayer());
        Assert.assertEquals(true,newcell.hasPlayer());


    }

    public void testSetUpOnLocation() //Method for setting location at game start
    {
          newcell.setPlayer();
          cell=newcell;
          blitzer.setActive();
          Assert.assertEquals(true, cell.hasPlayer());
          Assert.assertEquals(true, newcell.hasPlayer());
          Assert.assertEquals(true, blitzer.getActive());
     }
    public void testGetLocation()
    {

        Assert.assertEquals(1,cell.getRow());
        Assert.assertEquals(2,cell.getColumn());
        Assert.assertEquals(0,cell.getZone());
        Assert.assertEquals(false,cell.hasPlayer());

        }

 /*   public void testIsNear() {
         //location=new Cell(1,2);
         location=blitzer.getLocation();
         //location=new Cell(1,2);
         clickedCell=new Cell(3,4);
         Assert.assertEquals(false,blitzer.isNear(clickedCell));

    }
*/
  public void testSetActive() {
        //this.isActive = true;
        blitzer.setActive();
        Assert.assertEquals(true, blitzer.getActive());
        //setChanged();
        //notifyObservers();
    }


    public void testGetActive() {
       // return isActive;
      Assert.assertEquals(false,blitzer.getActive());
    }

// failed
/*    public void testToString() {
        //String playerInfo = "player in cell: " + location;
        //return playerInfo;
        //blackorc.toString();
        Assert.assertEquals("1",blitzer.getLocation().toString());

    }
*/

    public void testGetMa() {
        //return ma;
        Assert.assertEquals(1, blitzer.getMa());

    }

    public void testGetName() {
       // return name;
        Assert.assertEquals("Test Blitzer", blitzer.getName());
    }

    public void testGetSt() {
        //return st;
        Assert.assertEquals(2, blitzer.getSt());
    }

    public void testGetAv() {
        //return av;
        Assert.assertEquals(4, blitzer.getAv());
    }

    public void testIsHasMoved(){
        //return hasMoved;
        Assert.assertEquals(false,blitzer.hasMoved());
    }

    public void testGetAg() {
        Assert.assertEquals(3, blitzer.getAg());
    }


}
