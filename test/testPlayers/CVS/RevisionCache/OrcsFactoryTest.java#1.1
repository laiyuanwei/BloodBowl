/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package testPlayers;

import bloodbowl.model.players.OrcBlackorc;
import bloodbowl.model.players.OrcBlitzer;
import bloodbowl.model.players.OrcGoblin;
import bloodbowl.model.players.OrcLineman;
import bloodbowl.model.players.OrcThrower;
import bloodbowl.model.players.OrcTroll;
import bloodbowl.model.players.OrcsFactory;
import junit.framework.Assert;
import junit.framework.TestCase;

/**
 *
 * @author Yuanwei Lai
 */
public class OrcsFactoryTest extends TestCase {
    private OrcsFactory orcsfactory=null;
    
    public OrcsFactoryTest(String testName) {
        super(testName);
    }

    @Override
    protected void setUp() throws Exception {
        super.setUp();
        orcsfactory=new OrcsFactory();
    }

    @Override
    protected void tearDown() throws Exception {
        super.tearDown();
    }

    /**
     * Test of createCatcher method, of class OrcsFactory.
     */
    public void testCreateCatcher() {
        Assert.assertTrue(orcsfactory.createCatcher()==null);
    }

    /**
     * Test of createBlitzer method, of class OrcsFactory.
     */
    public void testCreateBlitzer() {
      Assert.assertTrue(orcsfactory.createBlitzer() instanceof OrcBlitzer);
    }

    /**
     * Test of createLineman method, of class OrcsFactory.
     */
    public void testCreateLineman() {
     Assert.assertTrue(orcsfactory.createLineman() instanceof OrcLineman);
    }

    /**
     * Test of createOgre method, of class OrcsFactory.
     */
    public void testCreateOgre() {
      Assert.assertTrue(orcsfactory.createOgre()==null);
    }

    /**
     * Test of createThrower method, of class OrcsFactory.
     */
    public void testCreateThrower() {
       Assert.assertTrue(orcsfactory.createThrower() instanceof OrcThrower);
    }

    /**
     * Test of createBlackorc method, of class OrcsFactory.
     */
    public void testCreateBlackorc() {
       Assert.assertTrue(orcsfactory.createBlackorc() instanceof OrcBlackorc);
    }

    /**
     * Test of createTroll method, of class OrcsFactory.
     */
    public void testCreateTroll() {
        Assert.assertTrue(orcsfactory.createTroll() instanceof OrcTroll);
    }

    /**
     * Test of createGoblin method, of class OrcsFactory.
     */
    public void testCreateGoblin() {
       Assert.assertTrue(orcsfactory.createGoblin() instanceof OrcGoblin);
    }

}
