/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package testPlayers;

import bloodbowl.model.map.Cell;
import bloodbowl.model.players.Goblin;
import bloodbowl.model.players.PlayerType;
import bloodbowl.model.players.Skills;
import junit.framework.Assert;
import junit.framework.TestCase;

import java.util.ArrayList;


/**
 *
 * @author Yuanwei Lai
 */
public class GoblinTest extends TestCase {
    ArrayList<Skills> skillsList = new ArrayList<Skills>();
    private Goblin goblin=null;
    private Cell cell=null;
    private Cell newcell=null;
    private Cell clickedCell=null;
    private Cell location=null;


    public GoblinTest(String testName) {
        super(testName);
    }

    @Override
    protected void setUp() throws Exception {
        super.setUp();
       // skill=Skills.PASS;
        goblin=new Goblin(1,2,3,4,10,skillsList,PlayerType.GOBLIN);
        cell=new Cell(1,2);
        newcell=new  Cell(1,2);
       // location=new Cell(1,2);
       // clickedCell=new Cell(3,4);
    }

    @Override
    protected void tearDown() throws Exception {
        super.tearDown();
    }

    // TODO add test methods here. The name must begin with 'test'. For example:
    // public void testHello() {}

    public void testResetMovementLeft(){
       goblin.resetMovementLeft();
       Assert.assertEquals(1, goblin.getMa());
    }

    public void testGetMaLeft(){
       Assert.assertEquals(1, goblin.getMa());
    }

    public void testDecreaseMovementLeft()
    {
        goblin.decreaseMovementLeft();
        Assert.assertEquals(0, goblin.getMa()-1);
        //maLeft--;
    }
   public void testCanMove()
    {
        boolean expected;
        if(goblin.getMa()>0)
            expected=true;
        else
            expected=false;
       Assert.assertEquals(expected,goblin.canMove());
    }
    public void testSetTeamID()
    {
        goblin.setTeamID(5);
        Assert.assertEquals(5,goblin.getTeamID());

        //teamID=id;
    }

    public void  testGetTeamID()
    {

        Assert.assertEquals(10, goblin.getTeamID());
        //return teamID;
    }
    public void testSetLocation()  //Method for relocation during gameplay
    {
        cell.resetPlayer();
        //cell = (PitchCell) newcell;
        cell.setPlayer();
        newcell.setPlayer();
        goblin.setActive();
        //blackorc.setChanged();
        Assert.assertEquals(true, cell.hasPlayer());
        Assert.assertEquals(true,newcell.hasPlayer());


    }

    public void testSetUpOnLocation() //Method for setting location at game start
    {
          newcell.setPlayer();
          cell=newcell;
          goblin.setActive();
          Assert.assertEquals(true, cell.hasPlayer());
          Assert.assertEquals(true, newcell.hasPlayer());
          Assert.assertEquals(true, goblin.getActive());
     }
    public void testGetLocation()
    {

        Assert.assertEquals(1,cell.getRow());
        Assert.assertEquals(2,cell.getColumn());
        Assert.assertEquals(0,cell.getZone());
        Assert.assertEquals(false,cell.hasPlayer());

        }

/*    public void testIsNear() {
         //location=new Cell(1,2);
         location=goblin.getLocation();
         //location=new Cell(1,2);
         clickedCell=new Cell(3,4);
         Assert.assertEquals(false,goblin.isNear(clickedCell));

    }
*/
  public void testSetActive() {
        //this.isActive = true;
        goblin.setActive();
        Assert.assertEquals(true, goblin.getActive());
        //setChanged();
        //notifyObservers();
    }


    public void testGetActive() {
       // return isActive;
      Assert.assertEquals(false,goblin.getActive());
    }

// failed
/*    public void testToString() {
        //String playerInfo = "player in cell: " + location;
        //return playerInfo;
        //blackorc.toString();
        Assert.assertEquals("1",goblin.getLocation().toString());

    }
*/

    public void testGetMa() {
        //return ma;
        Assert.assertEquals(1, goblin.getMa());

    }

   /* public void testGetName() {
       // return name;
        Assert.assertEquals("Test Goblin", goblin.getName());
    }
*/
    public void testGetSt() {
        //return st;
        Assert.assertEquals(2, goblin.getSt());
    }

    public void testGetAv() {
        //return av;
        Assert.assertEquals(4, goblin.getAv());
    }

    public void testHasMoved(){
        //return hasMoved;
        Assert.assertEquals(false,goblin.hasMoved());
    }

    public void testGetAg() {
        Assert.assertEquals(3, goblin.getAg());
    }


}
